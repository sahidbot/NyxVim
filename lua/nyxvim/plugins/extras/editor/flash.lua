require("lazy.core.util").warn(
  "`flash.nvim` is now the default jump plugin for **NyxVim**.\nPlease remove the `nyxvim.plugins.extras.editor.flash` import from your **lazy** config.",
  { title = "NyxVim" }
)

return {}
